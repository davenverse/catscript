#!./core/target/universal/stage/bin/catscript
// interpreter: IOApp.Simple
// scala: 3.0.0
// dependency: "org.http4s" %% "http4s-ember-client" % "0.23.0-RC1"

import cats.effect._
import cats.effect.std.Console

def run: IO[Unit] = Console[IO].println(s"Hello From IOApp.Simple!")
// def run(args: List[String]): IO[ExitCode] = Console[IO].println(s"Received $args- Hello from IOApp").as(ExitCode.Success)
